'use client';
import Layout from './admin/(protected)/layout';
import MainPage from './admin/(protected)/page';

const Home = () => {
  return (
    <Layout>
      <MainPage />
    </Layout>
  );
};

export default Home;
